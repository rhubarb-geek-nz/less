# rhubarb-geek-nz/less

Win32 implementation of `less` invoking a native editor.

Works with console based text editors.

Uses the `EDITOR` environment variable to select alternative editor.

```
C:>less < less.c
```
